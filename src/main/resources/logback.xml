<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-5level] %logger{32} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application logger -->
    <logger name="com.racer" level="DEBUG"/>

    <!-- Ktor framework - INFO to show CallLogging -->
    <logger name="io.ktor.server.plugins.calllogging" level="INFO"/>

    <!-- Suppress overly verbose logging -->
    <logger name="org.eclipse.jetty" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="org.apache.commons" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="com.mysql.cj" level="WARN"/>

    <!-- Suppress JVM logs -->
    <logger name="java" level="ERROR"/>
    <logger name="sun" level="ERROR"/>
    <logger name="jdk" level="ERROR"/>

    <!-- Root logger - INFO to show application logs -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>