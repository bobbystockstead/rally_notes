ktor:
  application:
    modules:
      - com.racing.ApplicationKt.module
  deployment:
    port: 8080

database:
  host: ${DB_HOST:localhost}
  port: ${DB_PORT:3306}
  name: ${DB_NAME:rally_notes}
  username: ${DB_USERNAME:rally_user}
  password: ${DB_PASSWORD:change_me}
  pool-size: ${DB_POOL_SIZE:20}
