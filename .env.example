# Copy this file to .env and replace values for your local machine.
# Ktor does not load .env automatically; source it before running the app.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=rally_notes
DB_USERNAME=rally_user
DB_PASSWORD=change_me
DB_POOL_SIZE=20

